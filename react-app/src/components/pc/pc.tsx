// @ts-ignore
import React from "react";
import useAllListStore from '../../store/allListStore.js'


function Pc() {
  const { list } = useAllListStore;

  return (
      <div> pc
      </div>
  );
}

export default Pc;
